NextAuth ใช้ NEXTAUTH_SECRET เป็นกุญแจเข้ารหัส (encrypt/decrypt) JWT

พอคุณเพิ่ม .env.local
➡️ กุญแจ “คงที่”
➡️ JWT ถอดรหัสได้
➡️ session อ่านได้
➡️ ทุกอย่างเลย “ได้ทันที”

------------------------------------------------------------

🔐 แล้ว NextAuth ใช้ JWT ตรงไหน?
ในโหมดที่คุณใช้ (Credentials, ไม่ใช้ DB)

NextAuth จะใช้:

JWT (JWE - encrypted)
stored in cookie


ไม่ใช่ session ใน database